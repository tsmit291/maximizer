{
  "name": "maximusprime",
  "main": "./lib/maximusprime",
  "version": "0.0.0",
  "description": "A short description of your package",
  "keywords": [],
  "activationCommands": {
    "atom-workspace": "maximusprime:toggle"
  },
  "scripts": {
    "compile": "babel --presets es2015 -d lib/ test/",
    "test": "npm run compile && mocha --require babel-core/register"
  },
  "repository": "https://github.com/atom/maximusprime",
  "license": "MIT",
  "engines": {
    "atom": ">=1.0.0 <2.0.0"
  },
  "dependencies": {
    "babel-polyfill": "^6.13.0",
    "chai": "^3.5.0",
    "mocha": "^3.0.2"
  },
  "devDependencies": {
    "babel-cli": "^6.14.0",
    "babel-core": "^6.14.0",
    "babel-preset-es2015": "^6.14.0"
  }
}
